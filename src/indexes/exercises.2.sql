/* -----------------------------------------------------------------------------
|
|    2. Listing indexes
|
|    * Uncomment the two lines below and save the file. The output will include
|      the indexes in the database and values for their usage.
|
|    * Add a command to reset the statistics before the two commands below.
|
|    * Save the file and check that all values for pg_stat_user_indexes are
|      zero.
|
|    * Go back to exercises.1.sql for the rest of the exercise.
|
*/ -----------------------------------------------------------------------------

-- TODO: Reset the statistics here

-- SELECT pg_sleep(1);
-- SELECT * FROM pg_stat_user_indexes;
