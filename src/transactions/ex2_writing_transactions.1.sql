/* -----------------------------------------------------------------------------
|
|    2. Writing Transactions
|
|    * Read the SELECT query below, and uncomment it.
|    * Switch to ex2_writing_transactions.2.sql and follow the instructions there.
|
*/ -----------------------------------------------------------------------------

SELECT pg_sleep(1);

-- SELECT *
-- FROM projects
-- LEFT JOIN work_items ON project_reference = projects.reference
-- WHERE projects.id = 500;
