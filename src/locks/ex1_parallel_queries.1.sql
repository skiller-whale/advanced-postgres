/* -----------------------------------------------------------------------------
|
|    1. Parallel Queries
|
|    * Read the queries below, uncomment them, and save this file.
|    * Switch to ex1_parallel_queries.2.sql and follow the instructions there.
|
*/ -----------------------------------------------------------------------------

-- SELECT pg_sleep(2);
-- SELECT id, name, clock_timestamp() FROM employees WHERE id = 1;
